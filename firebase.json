{
  "functions": [
    {
      "source": "dist/main/handlers/firebase",
      "codebase": "default",
      "predeploy": "yarn build",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ]
    }
  ],
  "storage": {
    "rules": "storage.rules"
  }
}
